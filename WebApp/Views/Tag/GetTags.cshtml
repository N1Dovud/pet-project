@model List<TagModel?>?;

<div>
    @foreach (var tag in @Model ?? []) {
        @await Html.PartialAsync("Partials/Tag/_TaskTagCard", new TagViewModel
        {
            Tag = tag,
            FiltersTasks = true,
            HasDeleteButton = false,
        })
    }
</div>